minLen, maxLen = float('inf'), float('-inf')
for record in LoL:
    if len(record[6]) > maxLen:
        maxLen = len(record[6])
    if len(record[6] < minLen:
for record in LoL:
    if len(record[6]) > maxLen:
        maxLen = len(record[6])
    if len(record[6]) < minLen:
        minLen = len(record[6])

